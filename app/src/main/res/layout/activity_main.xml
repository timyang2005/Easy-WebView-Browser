<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/colorBackground"
    android:fitsSystemWindows="true">

    <!-- 微型状态栏（仅全面屏设备显示） -->
    <LinearLayout
        android:id="@+id/mini_status_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:elevation="4dp"
        android:background="@drawable/mini_status_bar_background">

        <!-- 状态栏内容区 -->
        <LinearLayout
            android:id="@+id/mini_status_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="12dp"
            android:paddingEnd="12dp">

            <!-- HTTPS 安全标识 -->
            <ImageView
                android:id="@+id/mini_status_secure"
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:src="@drawable/ic_lock_insecure"
                android:scaleType="fitCenter"
                android:layout_marginEnd="4dp"
                android:importantForAccessibility="no" />

            <!-- 域名（点击展开完整网址） -->
            <TextView
                android:id="@+id/mini_status_domain"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="11sp"
                android:textColor="@color/mini_status_text"
                android:ellipsize="end"
                android:maxLines="1"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:paddingStart="4dp"
                android:paddingEnd="4dp" />

            <!-- 网页标题 -->
            <TextView
                android:id="@+id/mini_status_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1.5"
                android:textSize="11sp"
                android:textColor="@color/mini_status_text"
                android:ellipsize="end"
                android:maxLines="1"
                android:gravity="end"
                android:paddingStart="8dp"
                android:paddingEnd="4dp" />
        </LinearLayout>

        <!-- 网址编辑区（点击域名展开） -->
        <LinearLayout
            android:id="@+id/mini_status_url_editor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingBottom="4dp"
            android:visibility="gone">

            <EditText
                android:id="@+id/mini_status_url_edit"
                android:layout_width="0dp"
                android:layout_height="28dp"
                android:layout_weight="1"
                android:textSize="11sp"
                android:textColor="@color/mini_status_text"
                android:background="@drawable/mini_url_edit_background"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:singleLine="true"
                android:imeOptions="actionGo"
                android:inputType="textUri"
                android:selectAllOnFocus="true" />
        </LinearLayout>

        <!-- 加载进度线 -->
        <View
            android:id="@+id/mini_status_progress"
            android:layout_width="match_parent"
            android:layout_height="1.5dp"
            android:background="@color/blue_primary"
            android:visibility="gone" />
    </LinearLayout>

    <!-- WebView -->
    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- 首次启动界面 -->
    <LinearLayout
        android:id="@+id/start_screen"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="32dp">

        <TextView
            android:id="@+id/title_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textSize="24sp"
            android:textColor="@color/blue_primary"
            android:textStyle="bold"
            android:layout_marginBottom="24dp" />

        <EditText
            android:id="@+id/url_input"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/input_background"
            android:hint="@string/hint_url"
            android:inputType="textUri"
            android:imeOptions="actionGo"
            android:textSize="16sp"
            android:layout_marginBottom="16dp" />

        <Button
            android:id="@+id/btn_access"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_access"
            android:backgroundTint="@color/blue_primary"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:paddingLeft="40dp"
            android:paddingRight="40dp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/btn_open_file"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_open_file"
            android:backgroundTint="@color/blue_primary"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:paddingLeft="40dp"
            android:paddingRight="40dp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            android:layout_marginBottom="24dp" />

        <!-- 快捷方式区域 -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center">

            <!-- MetaCubeXD 快捷方式 -->
            <LinearLayout
                android:id="@+id/shortcut_metacubexd"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:padding="8dp">

                <ImageButton
                    android:id="@+id/btn_metacubexd"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:contentDescription="@string/metacubexd_name"
                    android:background="@drawable/circle_button_background"
                    android:src="@drawable/ic_metacubexd"
                    android:scaleType="centerInside"
                    android:padding="4dp"
                    android:elevation="4dp" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/metacubexd_name"
                    android:textSize="12sp"
                    android:textColor="@color/gray_text_dark"
                    android:layout_marginTop="4dp" />
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- 前进后退按钮（药丸形状） -->
    <LinearLayout
        android:id="@+id/nav_buttons"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@drawable/pill_button_background"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:layout_gravity="top|left"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:visibility="gone"
        android:elevation="8dp"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:contentDescription="@string/btn_back"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_back"
            android:scaleType="centerInside"
            android:padding="8dp" />

        <View
            android:layout_width="1dp"
            android:layout_height="24dp"
            android:background="#4DFFFFFF"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp" />

        <ImageButton
            android:id="@+id/btn_forward"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:contentDescription="@string/btn_forward"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_forward"
            android:scaleType="centerInside"
            android:padding="8dp" />
    </LinearLayout>

    <!-- 刷新按钮 -->
    <ImageButton
        android:id="@+id/btn_refresh"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:contentDescription="@string/btn_refresh"
        android:background="@drawable/circle_button_background"
        android:src="@drawable/ic_refresh"
        android:scaleType="centerInside"
        android:padding="12dp"
        android:layout_gravity="top|end"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="16dp"
        android:visibility="gone"
        android:elevation="8dp" />

    <!-- 主页按钮 -->
    <ImageButton
        android:id="@+id/btn_home"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:contentDescription="@string/btn_home"
        android:background="@drawable/circle_button_background"
        android:src="@drawable/ic_home"
        android:scaleType="centerInside"
        android:padding="12dp"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="16dp"
        android:visibility="gone"
        android:elevation="8dp" />

    <!-- 锁定遮罩层 -->
    <View
        android:id="@+id/lock_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/lock_overlay_bg"
        android:visibility="gone"
        android:elevation="10dp"
        android:clickable="true"
        android:focusable="true" />

    <!-- 锁定/解锁按钮 -->
    <ImageButton
        android:id="@+id/btn_lock"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:contentDescription="@string/btn_lock"
        android:background="@drawable/circle_button_background"
        android:src="@drawable/ic_lock_unlocked"
        android:scaleType="centerInside"
        android:padding="12dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:elevation="12dp" />

</FrameLayout>